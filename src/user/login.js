import React from 'react';
import axios from 'axios';
const Login = () => {
    

    return (
        // create a login page and form, when user submits the form it sends a username, password 
        // copy this from the navigation page "submitLogin"
        <div>
            Login
        </div>
    )
}
export default Login;
